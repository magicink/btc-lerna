name: build
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: install dependencies
        run: yarn
        shell: bash
      - name: test
        run: yarn test
        shell: bash
