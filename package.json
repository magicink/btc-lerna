{
  "name": "@brandontom/lerna",
  "private": true,
  "scripts": {
    "bootstrap": "npx lerna bootstrap --concurrency 1 --no-ci",
    "prebuild": "npm run clean",
    "build": "npm run bootstrap",
    "postbuild": "npm run test",
    "clean": "npx lerna clean --yes",
    "test": "npx lerna run test --stream --concurrency 1"
  },
  "dependencies": {
    "lerna": "^4.0.0"
  }
}
