{
  "name": "@brandontom/lerna",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*"
    ]
  },
  "scripts": {
    "prepare": "husky install",
    "test": "lerna run test"
  },
  "dependencies": {
    "cross-env": "^7.0.3",
    "dotenv": "^16.0.3",
    "husky": "^8.0.2",
    "lerna": "^6.5.1",
    "prettier": "^2.8.0",
    "rimraf": "^3.0.2",
    "sort-package-json": "^2.1.0"
  },
  "devDependencies": {
    "lint-staged": "^13.0.3",
    "typescript": "^4.9.5"
  }
}
